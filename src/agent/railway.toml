[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildTarget = "prod"

[deploy]
# Celery worker — no HTTP port exposed, no healthcheck path
startCommand = "celery -A celery_app worker --loglevel=warning --concurrency=4"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
